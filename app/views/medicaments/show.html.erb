<p id="notice"><%= notice %></p>
<div class="row-fluid">
  <div class="col-md-7 col-xs-7">
    <p>
      <strong>Nom:</strong>
      <%= @medicament.name %>
    </p>

    <p>
      <strong>Average price:</strong>
      <%= @medicament.average_price %>
    </p>

 <!--    <p>
      <strong>Forme:</strong>
      <%= @medicament.form %>
    </p>

    <p>
      <strong>Composition:</strong>
      <%= @medicament.composition %>
    </p>

    <p>
      <strong>Indication:</strong>
      <%= @medicament.indication %>
    </p>

    <p>
      <strong>Dose:</strong>
      <%= @medicament.dose %>
    </p>

    <p>
      <strong>Contraindication:</strong>
      <%= @medicament.contraindication %>
    </p> -->

    <button type="submit" class="btn btn-bobodoc" >
      <%= t('medicaments.add_reference') %>
    </button>  </div>

  <div class="col-md-5 col-xs-5 no_decoration">
    <p>Ce médicament est disponible dans les pharmacies suivantes:</p>
    <% for drugstore in @medicament.drugstores %>
      <li class="no_bullet"><%= link_to drugstore.address, drugstore %>
        - <%= Stock.find_by(drugstore_id: drugstore.id, medicament_id: @medicament.id).price %> €</li>

    <% end %>
  </div>
</div>