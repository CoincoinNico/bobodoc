<nav class="navbar-default navbar-bobodoc" role="navigation">
  <div class="container-fluid">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
      <button type="button" class="navbar-toggle" data-toggle="collapse" data-target="#bs-example-navbar-collapse-1">
        <span class="sr-only">Toggle navigation</span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
        <span class="icon-bar"></span>
      </button>
      <a class="navbar-brand" href=".."><i class="fa fa-plus-square"></i> Médoc'teurs</a>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse" id="bs-example-navbar-collapse-1">
      <ul class="nav navbar-nav navbar-right">
        <li class="active"><%= link_to "Médicaments", medicaments_path %></li>
        <li><%= link_to "Pharmacies", drugstores_path %></li>
        <% if user_signed_in? %>
          <% if user_signed_in? && current_user.provider == "facebook" %>
            <li class="dropdown">
              <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= image_tag current_user.picture_url, class: "img img-circle" %><b class="caret"></b></a>
          <% else %>
              <li class="dropdown">
                <a href="#" class="dropdown-toggle" data-toggle="dropdown"><%= current_user.name %><b class="caret"></b></a>
          <% end %>
          <ul class="dropdown-menu pull-right">
            <li><%= link_to '<i class="fa fa-medkit"></i> Médicaments'.html_safe, medicaments_path %></li>
            <li><%= link_to '<i class="fa fa-plus"></i> Pharmacies'.html_safe, drugstores_path %></li>
            <li class="divider"></li>
            <li><%= link_to '<i class="fa fa-sign-out"></i> Sign Out'.html_safe, destroy_user_session_path, method: :delete %></li>
          </ul>
        <% else %>
            <li><%= link_to '<i class="fa fa-sign-in"></i> Sign In'.html_safe, new_user_session_path %></li>
        <% end %>
        </li>
      </ul>
    </div><!-- /.navbar-collapse -->
  </div><!-- /.container-fluid -->
</nav>